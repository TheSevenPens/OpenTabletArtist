<UserControl x:Class="TabletDriverUX.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel MaxWidth="900" HorizontalAlignment="Left">
        <TextBlock Style="{StaticResource PageTitle}" Text="Console" />
        <TextBlock Style="{StaticResource PageSubtitle}" Text="Driver log output" />

        <Border Style="{StaticResource GlassPanel}" Padding="40">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" FontSize="40"
                           Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"
                           Opacity="0.5" Margin="0,0,0,16" />
                <TextBlock Text="Coming Soon" FontSize="16" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" />
                <TextBlock Text="Live log streaming from the OpenTabletDriver daemon will appear here."
                           FontSize="13" Foreground="{DynamicResource TextMutedBrush}"
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                           MaxWidth="320" Margin="0,8,0,0" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
