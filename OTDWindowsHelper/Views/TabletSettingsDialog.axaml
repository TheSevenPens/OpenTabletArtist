<Window x:Class="OtdWindowsHelper.Views.TabletSettingsDialog"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="using:OtdWindowsHelper.Controls"
        xmlns:conv="using:Avalonia.Data.Converters"
        Title="Tablet Settings"
        Width="700" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgBaseBrush}"
        CanResize="True">

    <Grid Margin="24" RowDefinitions="Auto,Auto,*">
        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,16">
            <Grid>
                <TextBlock Text="{Binding TabletName}" FontSize="20" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding RefreshWarning}" FontSize="11" FontStyle="Italic"
                           Foreground="#e87930" VerticalAlignment="Center" TextWrapping="Wrap"
                           HorizontalAlignment="Right" Margin="0,0,96,0" MaxWidth="360"
                           IsVisible="{Binding RefreshWarning, Converter={x:Static conv:StringConverters.IsNotNullOrEmpty}}" />
                <Button Content="Refresh" Command="{Binding RefreshCommand}" HorizontalAlignment="Right"
                        ToolTip.Tip="Reload settings from daemon"
                        Theme="{StaticResource GhostButton}" Padding="12,6" FontSize="12" />
            </Grid>
            <!-- Detected/connected status for this tablet (#132) -->
            <ctrl:TabletStatusBanner Text="{Binding DetectionText}" IsDetected="{Binding IsTabletDetected}" />
        </StackPanel>

        <!-- Tab buttons (hidden in the focused Pen Dynamics view, #133) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16"
                    IsVisible="{Binding !DynamicsOnly}">
            <RadioButton Content="Screen Mapping" IsChecked="True" x:Name="ScreenMappingTab"
                         Theme="{StaticResource TabRadioButton}" />
            <RadioButton Content="Pen Switches" x:Name="PenTipTab"
                         Theme="{StaticResource TabRadioButton}" Margin="4,0,0,0" />
            <RadioButton Content="ExpressKeys" x:Name="PenButtonsTab"
                         Theme="{StaticResource TabRadioButton}" Margin="4,0,0,0" />
            <RadioButton Content="Dynamics" x:Name="DynamicsTab"
                         Theme="{StaticResource TabRadioButton}" Margin="4,0,0,0" />
            <RadioButton Content="Filters" x:Name="FiltersTab"
                         Theme="{StaticResource TabRadioButton}" Margin="4,0,0,0" />
            <RadioButton Content="JSON" x:Name="JsonTab"
                         Theme="{StaticResource TabRadioButton}" Margin="4,0,0,0" />
        </StackPanel>

        <!-- Screen Mapping tab content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      IsVisible="{Binding #ScreenMappingTab.IsChecked}">
            <StackPanel>
                <!-- Output Mode -->
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="OUTPUT MODE" Classes="SectionLabel" />
                        <Grid ColumnDefinitions="Auto,*" Margin="0,8,0,0">
                            <TextBlock Grid.Column="0" Text="Windows Ink" FontSize="12" VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextSecondaryBrush}" Width="90" />
                            <ToggleSwitch Grid.Column="1" IsChecked="{Binding IsAbsoluteMode}"
                                          OnContent="Absolute" OffContent="Relative" />
                        </Grid>
                        <TextBlock Text="Absolute is recommended for drawing — it carries pressure &amp; tilt."
                                   FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="90,2,0,0"
                                   TextWrapping="Wrap" />
                        <TextBlock Text="Output mode needs to use Windows Ink for pressure and tilt to work"
                                   FontSize="10" Foreground="#e87930" FontStyle="Italic" Margin="0,8,0,0"
                                   TextWrapping="Wrap"
                                   IsVisible="{Binding CanFixOutputMode}" />
                        <Button Content="Fix" Theme="{StaticResource AccentButton}"
                                Command="{Binding FixOutputModeCommand}"
                                Padding="12,6" FontSize="11" Margin="0,6,0,0"
                                HorizontalAlignment="Left"
                                IsVisible="{Binding CanFixOutputMode}" />
                    </StackPanel>
                </Border>

                <!-- Area Mapping -->
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12"
                        IsVisible="{Binding HasAreaMapping}">
                    <StackPanel>
                        <Grid ColumnDefinitions="*,Auto,Auto">
                            <TextBlock Grid.Column="0" Text="AREA MAPPING" Classes="SectionLabel"
                                       VerticalAlignment="Center" />
                            <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshDisplaysCommand}"
                                    ToolTip.Tip="Refresh displays from Windows"
                                    Theme="{StaticResource GhostButton}" Padding="10,5" FontSize="11" Margin="0,0,6,0" />
                            <Button Grid.Column="2" Content="Display Settings"
                                    Command="{Binding OpenDisplaySettingsCommand}"
                                    ToolTip.Tip="Open Windows Display Settings"
                                    Theme="{StaticResource GhostButton}" Padding="10,5" FontSize="11" />
                        </Grid>
                        <TextBlock Text="Pick the display to map the tablet to, then Apply." FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,6,0,8" />
                        <ctrl:DisplayLayoutView Displays="{Binding Displays}"
                                                SelectedNumber="{Binding SelectedDisplayNumber, Mode=TwoWay}"
                                                Height="200" />
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0" Spacing="8">
                            <Button Content="Apply mapping" Command="{Binding ApplyDisplayCommand}"
                                    Theme="{StaticResource AccentButton}" Padding="14,7" FontSize="12"
                                    HorizontalAlignment="Left" />
                            <Button Content="Calibrate…" Command="{Binding CalibrateCommand}"
                                    IsEnabled="{Binding CanCalibrate}"
                                    Theme="{StaticResource GhostButton}" Padding="14,7" FontSize="12"
                                    ToolTip.Tip="Tap 4 corners to correct where the pen lands vs. where you see it (Absolute mode only)" />
                        </StackPanel>
                        <TextBlock Text="Calibration is available in Absolute mode."
                                   FontSize="10" Foreground="{DynamicResource TextMutedBrush}"
                                   Margin="0,4,0,0" IsVisible="{Binding !CanCalibrate}" />
                        <TextBlock Text="This tablet's calibration was made for a different display mapping — recalibrate to keep it accurate."
                                   FontSize="10" Foreground="{DynamicResource WarningBrush}" TextWrapping="Wrap"
                                   Margin="0,4,0,0" IsVisible="{Binding CalibrationStale}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Pen Tip & Eraser tab content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      IsVisible="{Binding #PenTipTab.IsChecked}">
            <StackPanel>
                <!-- Tip -->
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="PEN TIP" Classes="SectionLabel" />
                            <TextBlock FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,8,0,0"
                                       FontWeight="SemiBold" Text="{Binding TipBinding}" />
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0" IsVisible="{Binding TipAdaptive}">
                                <TextBlock Classes="success" />
                                <TextBlock Text="Adaptive Binding (recommended)" FontSize="10"
                                           Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="Not using recommended Adaptive Binding — pressure may not work correctly in drawing apps"
                                       FontSize="10" Foreground="#e87930" FontStyle="Italic" Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       IsVisible="{Binding CanFixTip}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Fix" Theme="{StaticResource AccentButton}"
                                Command="{Binding FixTipBindingCommand}"
                                Padding="16,8" FontSize="12" VerticalAlignment="Center"
                                IsVisible="{Binding CanFixTip}" />
                    </Grid>
                </Border>

                <!-- Eraser -->
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ERASER" Classes="SectionLabel" />
                            <TextBlock FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,8,0,0"
                                       FontWeight="SemiBold" Text="{Binding EraserBinding}" />
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0" IsVisible="{Binding EraserAdaptive}">
                                <TextBlock Classes="success" />
                                <TextBlock Text="Adaptive Binding (recommended)" FontSize="10"
                                           Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="Not using recommended Adaptive Binding"
                                       FontSize="10" Foreground="#e87930" FontStyle="Italic" Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       IsVisible="{Binding CanFixEraser}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Fix" Theme="{StaticResource AccentButton}"
                                Command="{Binding FixEraserBindingCommand}"
                                Padding="16,8" FontSize="12" VerticalAlignment="Center"
                                IsVisible="{Binding CanFixEraser}" />
                    </Grid>
                </Border>

                <!-- Pen Buttons (the buttons on the pen barrel) -->
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="PEN BUTTONS" Classes="SectionLabel" />
                            <ItemsControl ItemsSource="{Binding PenButtons}" Margin="0,8,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,3,0,3">
                                            <TextBlock Text="{Binding Label}" FontSize="11" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextSecondaryBrush}" Width="80" />
                                            <TextBlock Text="{Binding Name}" FontSize="11"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="No pen buttons configured" FontSize="11" FontStyle="Italic"
                                       Foreground="{DynamicResource TextMutedBrush}" Margin="0,8,0,0"
                                       IsVisible="{Binding NoPenButtons}" />
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0" IsVisible="{Binding PenButtonsAdaptive}">
                                <TextBlock Classes="success" />
                                <TextBlock Text="All using Adaptive Binding (recommended)" FontSize="10"
                                           Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="Not all pen buttons use recommended Adaptive Binding"
                                       FontSize="10" Foreground="#e87930" FontStyle="Italic" Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       IsVisible="{Binding CanFixPenButtons}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Fix All" Theme="{StaticResource AccentButton}"
                                Command="{Binding FixPenButtonsCommand}"
                                Padding="16,8" FontSize="12" VerticalAlignment="Top" Margin="12,0,0,0"
                                IsVisible="{Binding CanFixPenButtons}" />
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- ExpressKeys tab content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      IsVisible="{Binding #PenButtonsTab.IsChecked}">
            <StackPanel>
                <!-- Aux Buttons -->
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="AUXILIARY BUTTONS" Classes="SectionLabel" />
                        <ItemsControl ItemsSource="{Binding AuxButtons}" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,3,0,3">
                                        <TextBlock Text="{Binding Label}" FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" Width="80" />
                                        <TextBlock Text="{Binding Name}" FontSize="11"
                                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="No auxiliary buttons configured" FontSize="11" FontStyle="Italic"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,8,0,0"
                                   IsVisible="{Binding NoAuxButtons}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Dynamics tab content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      IsVisible="{Binding #DynamicsTab.IsChecked}">
            <StackPanel IsEnabled="{Binding CanEditPressure}">
                <!-- Master On/Off as a header row (not a card). -->
                <Grid ColumnDefinitions="*,Auto" Margin="2,2,2,10">
                    <TextBlock Grid.Column="0" Text="PEN DYNAMICS" Classes="SectionLabel" VerticalAlignment="Center" />
                    <ToggleSwitch Grid.Column="1" VerticalAlignment="Center"
                                  OnContent="On" OffContent="Off"
                                  IsChecked="{Binding PressureCurveEnabled}" />
                </Grid>

                <!-- Curve + smoothing only apply when Pen Dynamics is On — dim them when it's Off. -->
                <StackPanel IsEnabled="{Binding PressureCurveEnabled}">

                    <!-- Pressure curve: chart + presets + softness + dead zone + node read-outs -->
                    <Border Classes="GlassPanel" Padding="14" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="PRESSURE CURVE" Classes="SectionLabel" />

                            <!-- Two columns: compact chart on the left, controls on the right. -->
                            <Grid ColumnDefinitions="Auto,*" Margin="0,8,0,0">
                                <!-- Compact 1:1 chart; the green dot tracks live pen pressure (#102). -->
                                <ctrl:PressureCurveChart Grid.Column="0" Curve="{Binding Curve, Mode=TwoWay}"
                                                         LivePressure="{Binding LivePressure}"
                                                         Width="180" Height="180"
                                                         VerticalAlignment="Top" Margin="0,0,16,0" />

                                <StackPanel Grid.Column="1">
                                    <Grid ColumnDefinitions="Auto,*" Margin="0,0,0,8">
                                        <TextBlock Grid.Column="0" Text="Presets" FontSize="12" VerticalAlignment="Center"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" Width="70" />
                                        <WrapPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="Linear" Command="{Binding ApplyPresetCommand}" CommandParameter="linear"
                                                    Theme="{StaticResource GhostButton}" Padding="12,5" FontSize="11" Margin="0,0,6,4" />
                                            <Button Content="Soft" Command="{Binding ApplyPresetCommand}" CommandParameter="soft"
                                                    Theme="{StaticResource GhostButton}" Padding="12,5" FontSize="11" Margin="0,0,6,4" />
                                            <Button Content="Firm" Command="{Binding ApplyPresetCommand}" CommandParameter="firm"
                                                    Theme="{StaticResource GhostButton}" Padding="12,5" FontSize="11" Margin="0,0,6,4" />
                                            <Button Content="Reset" Command="{Binding ResetCurveCommand}"
                                                    Theme="{StaticResource GhostButton}" Padding="12,5" FontSize="11" Margin="0,0,0,4"
                                                    ToolTip.Tip="Reset the curve to the linear default" />
                                        </WrapPanel>
                                    </Grid>

                                    <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,8">
                                        <TextBlock Grid.Column="0" Text="Softness" FontSize="12" VerticalAlignment="Center"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" Width="70"
                                                   ToolTip.Tip="Positive = lighter touch (concave) · Negative = firmer (convex)" />
                                        <Slider Grid.Column="1" Minimum="-0.95" Maximum="0.95"
                                                Value="{Binding Softness, Mode=TwoWay}" VerticalAlignment="Center"
                                                ToolTip.Tip="Positive = lighter touch (concave) · Negative = firmer (convex)" />
                                        <TextBlock Grid.Column="2" Text="{Binding SoftnessText}" FontSize="12"
                                                   FontFamily="Consolas, Cascadia Code" VerticalAlignment="Center"
                                                   Width="40" TextAlignment="Right"
                                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                                    </Grid>

                                    <CheckBox Content="Cut below input minimum (dead zone)"
                                              IsChecked="{Binding CutBelowMinimum}" FontSize="12" Margin="0,0,0,8"
                                              Foreground="{DynamicResource TextSecondaryBrush}"
                                              ToolTip.Tip="Below the input minimum, produce zero output (a dead zone) instead of holding at the output minimum." />

                                    <Grid ColumnDefinitions="Auto,*,*" RowDefinitions="Auto,Auto,Auto">
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="Input" Classes="SectionLabel"
                                                   HorizontalAlignment="Center" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Output" Classes="SectionLabel"
                                                   HorizontalAlignment="Center" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Min node" FontSize="12" Width="70"
                                                   VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding InputMinimumText}" FontSize="12"
                                                   FontFamily="Consolas, Cascadia Code" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding OutputMinimumText}" FontSize="12"
                                                   FontFamily="Consolas, Cascadia Code" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Max node" FontSize="12" Width="70"
                                                   VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding InputMaximumText}" FontSize="12"
                                                   FontFamily="Consolas, Cascadia Code" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}" />
                                        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding OutputMaximumText}" FontSize="12"
                                                   FontFamily="Consolas, Cascadia Code" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}" />
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Smoothing -->
                    <Border Classes="GlassPanel" Padding="14" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="SMOOTHING (JITTER REDUCTION)" Classes="SectionLabel"
                                       ToolTip.Tip="EMA smoothing trades a little lag for steadier output. Position steadies wobbly lines; pressure evens out pressure jitter." />
                            <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,8,0,4">
                                <TextBlock Grid.Column="0" Text="Position" FontSize="12" VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextSecondaryBrush}" Width="80" />
                                <Slider Grid.Column="1" Minimum="0" Maximum="1"
                                        Value="{Binding PositionSmoothing, Mode=TwoWay}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding PositionSmoothingText}" FontSize="12"
                                           FontFamily="Consolas, Cascadia Code" VerticalAlignment="Center"
                                           Width="44" TextAlignment="Right"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                            </Grid>
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <TextBlock Grid.Column="0" Text="Pressure" FontSize="12" VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextSecondaryBrush}" Width="80" />
                                <Slider Grid.Column="1" Minimum="0" Maximum="1"
                                        Value="{Binding PressureSmoothing, Mode=TwoWay}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding PressureSmoothingText}" FontSize="12"
                                           FontFamily="Consolas, Cascadia Code" VerticalAlignment="Center"
                                           Width="44" TextAlignment="Right"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Order (compact one-row card) -->
                    <Border Classes="GlassPanel" Padding="14" Margin="0,0,0,10">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Grid.Column="0" Text="ORDER" Classes="SectionLabel" VerticalAlignment="Center"
                                       ToolTip.Tip="Whether smoothing runs after the curve (default) or before it." />
                            <ToggleSwitch Grid.Column="1" IsChecked="{Binding SmoothAfterCurve}"
                                          OnContent="Curve → Smooth" OffContent="Smooth → Curve" VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Filters tab content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      IsVisible="{Binding #FiltersTab.IsChecked}">
            <StackPanel>
                <Border Classes="GlassPanel" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="FILTERS" Classes="SectionLabel" />
                        <TextBlock Text="{Binding FiltersText}" FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" Margin="0,8,0,0"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- JSON tab content -->
        <Border Grid.Row="2" Classes="GlassPanel" Padding="16"
                IsVisible="{Binding #JsonTab.IsChecked}">
            <TextBox Text="{Binding RawJson, Mode=OneWay}" IsReadOnly="True"
                     FontFamily="Cascadia Code, Consolas, Courier New" FontSize="11"
                     Foreground="{DynamicResource TextSecondaryBrush}"
                     Background="Transparent" BorderThickness="0"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True" />
        </Border>
    </Grid>
</Window>
