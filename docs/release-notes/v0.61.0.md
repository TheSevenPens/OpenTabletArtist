## Fixed

- **The Console has one scrollbar, not two.** The toolbar is pinned and only the log
  scrolls.
- **A busy log no longer bogs the app down.** Messages are batched rather than each one
  redrawing the view.
