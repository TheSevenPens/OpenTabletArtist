## What's new

- **A MEDIA tab in the binding editor** — play/pause, next, volume and the rest — plus
  the numpad's `=` key, which was missing.
- **Calibration says which display it is calibrating,** and the overlay follows if you
  remap to a different one partway through.
