## Changed

**Per-app profile switching is hidden behind a flag and turned off.** It needs more work
before it can be relied on, and a switching feature that sometimes doesn't is worse than
one that isn't there.
