## What's new

- **The Start-menu shortcut is a checkbox** — tick to create it, untick to remove it —
  instead of a button that only worked one way.
- **A softness bend node on the pressure curve,** with the sliders cleaned up and
  pressure separated from movement.
- **An inline Discord help link** on Home.
